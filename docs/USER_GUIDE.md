# User Documentation

## Quick Start
1. Install dependencies with Poetry.
2. Run `py -m poetry run python run.py`.
3. Inspect logs under `logs/application.json`.

## Product Model

- The application uses canonical knowledge from the Fusion Database.
- Illustration Metadata is maintained separately from canonical character knowledge.
- Detailed frozen behavior and architecture are documented in `docs/ARCHITECTURE.md`.
